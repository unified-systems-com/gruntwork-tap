{
  "version": 1,
  "description": "gruntwork CI record (req-boot-bootstrap-ci-record): the stack this plugin's own tests run in. Installs gruntwork alone (it declares no dependencies); the consumer flips self to editable so the checkout under test runs, not the pinned revision. Population is empty: v0 ships no GRIFT. Deliberately excluded: every other plugin, all credentials, anything that reaches the network.",
  "install": {
    "plugins": [
      {
        "slug": "gruntwork",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/gruntwork-tap",
          "rev": "a7c0585c94f5b18045808ee4aa256a33005fe546"
        },
        "note": "Self, pinned at the bootstrap commit until the first tagged release; the consumer (plugin-ci, or `spawn --dev-plugins gruntwork`) substitutes the checkout under test via tap.dev_workspace (req-boot-bootstrap-ci-record-4)."
      }
    ]
  },
  "population": {
    "on_failure": "abort",
    "steps": []
  }
}
